LocalRuntimeConfig

runtime_id: string
node_id: string
agent_id: string
environment: string
server_endpoint: string | null
protocol_version: string
heartbeat_interval_seconds: integer
offline_mode: boolean
local_execution_enabled: boolean
cloud_execution_enabled: boolean

Environments
development
testing
production

RuntimeModes
online
offline
hybrid

ConfigurationRules
server_endpoint_must_be_verified
protocol_version_must_be_supported
credentials_must_not_be_stored_in_plaintext
secrets_must_not_be_logged
